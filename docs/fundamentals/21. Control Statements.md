## 21. Control Statements

`if`

```java
if(condition){
	// statements
}else if(condition){
	// statements
}else{
	// statements
}
```

`switch`

```java
switch(expression){
	case value1:
		// statements
		break;
	case value2:
		// statements
		break;
	default:
		// statements
}
```